name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Cancel in-progress runs on the same branch (except main — don't abort a release mid-flight)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write          # semantic-release: push version commit + tag
  packages: write          # push image to GHCR
  id-token: write          # build provenance / SBOM attestations
  security-events: write   # upload Trivy SARIF to GitHub Security tab

# ── Job 1: Build ─────────────────────────────────────────────────────────────
# Installs all dependencies and verifies the app is importable.
# Acts as a gate: lint and test only run if this passes.
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - name: Verify dependencies
        run: pip check
      - name: Audit dependencies for known vulnerabilities
        run: pip-audit -r requirements.txt
      - name: Verify app is importable
        run: python -c "import app"

# ── Job 2: Dependency Review (PRs only) ──────────────────────────────────────
# Flags newly introduced dependencies with known vulnerabilities before merge.
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v6
      - uses: actions/dependency-review-action@v4

# ── Job 3: Lint ──────────────────────────────────────────────────────────────
# black → ruff (auto-fix + commit) → pylint → mypy
  lint:
    name: Lint & Format (black → ruff → pylint → mypy)
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          # Use a token that can push back to the branch (needed for auto-fix commits).
          # Falls back to GITHUB_TOKEN which works for branch pushes but not fork PRs.
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - run: pip install -r requirements.txt -r requirements-dev.txt

      - name: black format
        run: black .

      - name: ruff fix
        run: |
          ruff check --fix .
          ruff format .

      - name: Commit fixes if any
        if: github.event.pull_request.head.repo.full_name == github.repository || github.event_name != 'pull_request'
        run: |
          if ! git diff --quiet; then
            git config user.name  "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add -u
            git commit -m "style: auto-fix black/ruff issues"
            git push
          fi

      - name: pylint
        run: pylint app.py tests/

      - name: bandit
        run: bandit -r app.py

      - name: mypy
        run: mypy app.py

# ── Job 4: Unit Tests ────────────────────────────────────────────────────────
# Runs in parallel with lint (both gate on build, not each other).
# Matrix across Python 3.11–3.13 to confirm cross-version compatibility.
# Coverage is uploaded to Codecov from the 3.12 run only.
  test:
    name: Unit Tests (pytest / Python ${{ matrix.python-version }})
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - run: pytest --tb=short -v --cov=app --cov-fail-under=80 --cov-report=term-missing --cov-report=xml
      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          files: coverage.xml

# ── Job 5: Semantic Release (main only) ──────────────────────────────────────
# Analyzes commits since the last tag (Angular convention: fix/feat/BREAKING CHANGE),
# bumps the version in pyproject.toml, commits, tags, and creates a GitHub release.
  release:
    name: Semantic Release
    needs: [lint, test]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    outputs:
      released: ${{ steps.semrel.outputs.released }}
      version: ${{ steps.semrel.outputs.version }}
      tag: ${{ steps.semrel.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Python Semantic Release
        id: semrel
        uses: python-semantic-release/python-semantic-release@v9
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

# ── Job 6: Dockerise ─────────────────────────────────────────────────────────
# Build → smoke test → Trivy scan → push to GHCR → sign + attest SBOM
  docker:
    name: Dockerise
    needs: [lint, test, release]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Run when quality gates pass regardless of whether 'release' ran (PRs skip it)
    if: |
      always() &&
      needs.lint.result == 'success' &&
      needs.test.result == 'success' &&
      (needs.release.result == 'success' || needs.release.result == 'skipped')
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0   # needed so metadata-action can read git tags

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Compute image tags:
      #   on main with a new release → <version>, sha-<sha>, latest
      #   on main without a new release → sha-<sha>, latest
      #   on PRs → sha-<sha> only (image not pushed)
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=${{ needs.release.outputs.version }},enable=${{ needs.release.outputs.released == 'true' }}
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}

      # ── Build the test stage ──────────────────────────────────────────────
      # Runs ruff, mypy, and pytest inside the container to confirm the image
      # build environment is clean — separate from the native CI jobs above.
      - name: Build Docker test stage
        uses: docker/build-push-action@v7
        with:
          context: .
          target: test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── Build runtime image locally ───────────────────────────────────────
      # Loads into the local Docker daemon for smoke test and Trivy.
      # Uses the GHA layer cache warmed by the test-stage build above.
      - name: Build runtime image (local load)
        uses: docker/build-push-action@v7
        with:
          context: .
          target: runtime
          load: true
          tags: inventory-service:ci
          cache-from: type=gha

      # ── Smoke test ────────────────────────────────────────────────────────
      - name: Smoke test
        run: |
          trap 'docker rm -f inventory-ci 2>/dev/null || true' EXIT

          docker run -d \
            --name inventory-ci \
            -p 5000:5000 \
            -e OTEL_SDK_DISABLED=true \
            inventory-service:ci

          echo "Waiting for app to be ready..."
          timeout 30 bash -c 'until curl -sf http://localhost:5000/healthz > /dev/null; do sleep 1; done'
          echo "App is ready."

          curl -sf http://localhost:5000/ | jq -e '.status == "ok"'
          curl -sf http://localhost:5000/items | jq -e 'type == "array"'
          curl -sf http://localhost:5000/items/1 | jq -e '.id == 1'

          status=$(curl -o /dev/null -sw "%{http_code}" http://localhost:5000/items/9999)
          [[ "$status" == "404" ]] || { echo "Expected 404 for /items/9999, got $status"; exit 1; }

          curl -sf http://localhost:5000/process | jq -e 'has("result")'

      # ── Trivy: fail on unfixed CRITICAL/HIGH ──────────────────────────────
      - name: Trivy vulnerability scan
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          image-ref: inventory-service:ci
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      # ── Trivy: SARIF upload to GitHub Security tab ────────────────────────
      - name: Trivy SARIF report
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          image-ref: inventory-service:ci
          format: sarif
          output: trivy.sarif

      - name: Upload Trivy SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy.sarif

      # ── SBOM ──────────────────────────────────────────────────────────────
      - name: Generate SBOM
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          image-ref: inventory-service:ci
          format: spdx-json
          output: sbom.spdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v7
        with:
          name: sbom
          path: sbom.spdx.json
          retention-days: 90

      # ── Push to GHCR (main only) ──────────────────────────────────────────
      # Re-runs the build with push=true. The GHA layer cache makes this ~instant.
      - name: Push to GHCR
        id: push
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/build-push-action@v7
        with:
          context: .
          target: runtime
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          platforms: linux/amd64
          provenance: true
          sbom: true

      # ── Cosign: keyless sign + SBOM attestation (main only) ───────────────
      # Signing uses GitHub OIDC — no private key needed.
      # Verify with:
      #   cosign verify \
      #     --certificate-identity-regexp="https://github.com/<owner>/<repo>/.*" \
      #     --certificate-oidc-issuer="https://token.actions.githubusercontent.com" \
      #     ghcr.io/<owner>/<repo>:<tag>
      - name: Install Cosign
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: sigstore/cosign-installer@v3

      - name: Sign image
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          cosign sign --yes \
            ghcr.io/${{ github.repository }}@${{ steps.push.outputs.digest }}

      - name: Attest SBOM
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          cosign attest --yes \
            --predicate sbom.spdx.json \
            --type spdxjson \
            ghcr.io/${{ github.repository }}@${{ steps.push.outputs.digest }}
